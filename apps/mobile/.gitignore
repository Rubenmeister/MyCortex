# Credenciales de submit a tiendas — NUNCA commitear la clave.
# Se ignora cualquier JSON/keystore dentro de credentials/, pero se conserva
# el README con las instrucciones.
credentials/*.json
credentials/*.keystore
!credentials/README.md
*.keystore
google-play-service-account.json

# Expo / EAS
.expo/
dist/
web-build/
*.orig.*
