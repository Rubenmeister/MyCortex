# cortex-context worker — propone hechos estables para la capa 1 (constitución).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Requerida: las propuestas razonan con Claude.
ANTHROPIC_API_KEY=
# Ventana de material a analizar por workspace (días).
CONTEXT_LOOKBACK_DAYS=30
# Máximo de nodos por corrida y workspace.
CONTEXT_MAX_NODES=60
