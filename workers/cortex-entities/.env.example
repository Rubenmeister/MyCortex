# cortex-entities worker — puebla el grafo de entidades (personas/proyectos/orgs).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Requerida: la extracción razona con Claude.
ANTHROPIC_API_KEY=
# Ventana de material a analizar por workspace (días). Subir para backfill.
ENTITIES_LOOKBACK_DAYS=30
# Máximo de nodos por corrida y workspace (tope del prompt al LLM).
ENTITIES_MAX_NODES=80
